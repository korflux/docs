---
title: "Sistema de Tickets (/ticket)"
description: "Gestão de atendimento organizada com categorias dinâmicas e logs automáticos."
---

O sistema de tickets do zNyx é uma solução robusta para suporte ao cliente, permitindo que sua equipe gerencie dúvidas, denúncias e solicitações de forma centralizada e profissional. Um suporte eficiente é o que diferencia servidores amadores de grandes comunidades de Minecraft.

Com o zNyx, cada atendimento é transformado em um canal privado, garantindo a privacidade do usuário e a organização da equipe de Staff.

---

## ⚙️ Configuração Estrutural

Antes de disponibilizar o suporte, é necessário definir o "esqueleto" do sistema.

### `configurar`
Este subcomando define os parâmetros fundamentais para o funcionamento. Sem ele, o bot não saberá onde criar os canais ou onde enviar os registros.

<ParamField query="categoria" type="Snowflake (ID)" required>
  O ID da categoria onde os novos tickets serão criados.
</ParamField>

<ParamField query="logs" type="Canal (#)" required>
  O canal onde os Transcripts (logs) serão enviados.
</ParamField>

*   **Exemplo de Uso:**
```bash
/ticket configurar categoria:123456789 logs:#logs-suporte
```

---

## 🛠️ Gestão de Categorias

As categorias permitem que o usuário escolha o motivo do contato (ex: Suporte Técnico, Financeiro, Denúncia).

### `categorias adicionar`
Insere uma nova opção no menu de seleção do painel de tickets.

*   **Destaque:** Você pode usar Emojis customizados para tornar o menu mais atraente.
<ParamField query="emoji" type="Emoji" required>
  O ícone que aparecerá ao lado do título.
</ParamField>

<ParamField query="titulo" type="String" required>
  O nome da categoria (ex: Suporte Geral).
</ParamField>

### `categorias listar`
Exibe todas as categorias atualmente cadastradas para que você possa revisar ou IDs para exclusão.

---

## 🚀 Operação e Controle

### `iniciar`
Envia o Painel de Atendimento (Embed com botão) para o canal desejado.

*   **Uso:** `/ticket iniciar`
*   **Recomendação:** Utilize em um canal limpo, preferencialmente chamado `#suporte` ou `#ajuda`.

---

## 🔒 Encerrando Atendimentos

### `/fecharticket`
Este é um comando independente e um dos mais importantes do módulo. Ele encerra o atendimento atual de forma definitiva.

*   **Transcripts Automáticos:** No momento em que este comando é executado, o zNyx gera um arquivo HTML visualmente idêntico ao chat do Discord e o envia para o canal de logs configurado.
*   **Segurança:** Garante que a conversa seja salva para futuras auditorias antes do canal ser deletado.

*   **Exemplo de Uso:**
```bash
/fecharticket
```

---

## 💎 Diferenciais Técnicos

<AccordionGroup>
  <Accordion title="Transcripts em HTML">
    Ao contrário de logs em TXT simples, nossos Transcripts mantêm a formatação, cores, imagens e avatares, permitindo uma leitura fluida do atendimento.
  </Accordion>
  <Accordion title="Permissões Dinâmicas">
    O bot gerencia as permissões do canal automaticamente, permitindo que apenas o Autor e a Staff configurada vejam o conteúdo.
  </Accordion>
</AccordionGroup>

<Note>
  **Atenção às Permissões:**
  - Comandos de configuração (`/ticket configurar`, `iniciar`, etc) exigem que o usuário seja um **Administrador**.
  - O comando `/fecharticket` é a única exceção: as permissões para executá-lo são definidas dentro das configurações do próprio sistema de tickets, permitindo que cargos específicos de Staff (mesmo sem permissão de Admin) possam encerrar os atendimentos.
</Note>

