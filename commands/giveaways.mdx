---
title: Sorteios
description: 'Engajamento e crescimento com sorteios auditáveis'
---

<Note>Este módulo é exclusivo do **Plano Pro**.</Note>

Maximize a retenção e o engajamento da sua comunidade. O sistema de sorteios do zNyx foi desenhado para ser transparente, seguro contra fraudes e visualmente impactante.

## 🎲 `/sortear`
<span className="api-method-tag post">PRO</span>

Inicia um novo sorteio totalmente configurável, desde as regras de participação até a identidade visual.

### Regras e Funcionamento
| Opção | Tipo | Obrigatoriedade | Descrição |
| :--- | :--- | :--- | :--- |
| `ganhadores` | Número | **Obrigatório** | Quantidade de vencedores (ex: `1`, `3`). |
| `data_final` | Texto | **Obrigatório** | Data de encerramento no formato `DD/MM/AAAA HH:MM` (ex: `25/12/2026 23:59`). |
| `cargos` | Cargo (@) | Opcional | Restringe o sorteio apenas para quem possui este cargo (ex: @Vip). Se vazio, é público. |
| `fraude` | Booleano | Opcional | Se `True` (padrão), impede contas novas (criadas há menos de 7 dias) de participar. |

### Personalização Visual
| Opção | Tipo | Obrigatoriedade | Descrição |
| :--- | :--- | :--- | :--- |
| `titulo` | Texto | Opcional | Título do embed do sorteio. |
| `imagem` | Link (URL) | Opcional | URL de um banner ou GIF para ilustrar o prêmio. |
| `cor` | Texto (HEX) | Opcional | Cor da lateral do embed (ex: `#FFD700`). |
| `rodape` | Texto | Opcional | Mensagem personalizada no rodapé. |

---

## 🔄 `/reroll`
<span className="api-method-tag patch">PRO</span>

Precisa refazer o sorteio? O sistema de reroll é inteligente e garante que não haja repetição de vencedores.

<Warning>Ao executar um reroll, o sistema adiciona os ganhadores anteriores a uma "lista negra" temporária, garantindo que o prêmio vá para um novo membro.</Warning>

### Argumentos
| Opção | Tipo | Obrigatoriedade | Descrição |
| :--- | :--- | :--- | :--- |
| `mensagem_id` | ID | **Obrigatório** | O ID da mensagem do sorteio original (Ative o Modo Desenvolvedor do Discord). |
| `ganhadores` | Número | Opcional | Quantidade de novos nomes a sortear. Se não informado, usa a mesma quantidade do sorteio original. |