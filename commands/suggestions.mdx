---
title: Sugestões e Avaliações
sidebarTitle: Sugestões
description: Colete feedbacks e avaliações da comunidade
---

Democratize as decisões do servidor e ouça seus membros.

## `/sugestao`
<span className="api-method-tag get">PLUS</span>

Qualquer membro pode usar para enviar uma ideia. O bot formata um embed com votação automática (👍/👎).

**Como funciona:**
1. Membro envia `/sugestao texto: "Adicionar minigame X"`.
2. Bot posta no canal de `analise`.
3. Membros votam.

---

## `/resultado`
<span className="api-method-tag patch">PLUS</span>

Aprova ou reprova uma sugestão.

**Opções:**
<ParamField query="sugestao_id" type="string" required>
  ID da mensagem da sugestão.
</ParamField>
<ParamField query="decisao" type="string" required>
  `Aprovar` ou `Reprovar` (move para o canal de resultados com a cor correspondente).
</ParamField>
<ParamField query="resposta" type="string">
  Comentário da equipe sobre a decisão (ex: "Vamos implementar em breve!").
</ParamField>

---

## `/avaliacao`
<span className="api-method-tag post">PRO</span>

Cria um painel de "Avalie nosso Atendimento/Servidor" com notas de 1 a 5 estrelas.

**Opções:**
<ParamField query="configurar" type="string">
  Define canais de embed e logs.
</ParamField>
<ParamField query="anonimo" type="boolean">
  Se `True`, oculta quem enviou a avaliação.
</ParamField>

---

## `/responder`
<span className="api-method-tag patch">PRO</span>

Permite que a staff responda publicamente a uma avaliação recebida.

**Opções:**
<ParamField query="avaliacao_id" type="string" required>
  ID da mensagem da avaliação.
</ParamField>
<ParamField query="resposta" type="string" required>
  Texto da resposta da equipe.
</ParamField>
