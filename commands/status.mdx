---
title: "Status de Servidores (/status)"
description: "Monitoramento em tempo real de servidores Minecraft direto no seu Discord."
---

O comando `/status` permite que você crie painéis dinâmicos que informam à sua comunidade se o seu servidor está online, quantos jogadores estão conectados e qual a versão ativa.

---

## 📊 Configuração do Painel

### `configurar`
Define os endereços dos servidores que serão monitorados.

*   **Uso:** `/status configurar servidores:<IP:Porta>`
*   **Parâmetros:**
    *   `servidores`: O endereço IP do seu servidor Minecraft. Você pode incluir a porta se for diferente da padrão (25565).
    *   **Plano Plus:** Permite monitorar **1 servidor**.
    *   **Plano Pro:** Permite monitorar **ilimitados servidores** no mesmo painel.
*   **Exemplo:** `/status configurar servidores:jogar.znyx.com`

---

### `iniciar`
Envia o painel de status para o canal e inicia o ciclo de atualização.

*   **Uso:** `/status iniciar`
*   **Ciclo de Atualização:** O bot consulta o servidor e atualiza o embed automaticamente a cada **10 minutos**. Isso garante que as informações estejam sempre frescas sem sobrecarregar sua API.

---

### `parar`
Interrompe o monitoramento e remove o painel de status do canal.

*   **Uso:** `/status parar`
*   **Nota:** Use este comando se precisar reconfigurar os IPs ou mover o painel de canal.

---

## 🛠️ Modo Manutenção

### `manutencao ligar`
<Tooltip tip="Recurso exclusivo do Plano Pro">**RECURSO PRO**</Tooltip>

Ativa um estado visual de manutenção no painel de status, informando que o servidor está temporariamente inacessível para ajustes.

*   **Uso:** `/status manutencao ligar texto:<Motivo>`
*   **Vantagem:** Evita que os jogadores pensem que o servidor caiu por problemas técnicos quando, na verdade, é uma pausa planejada.
*   **Exemplo:** `/status manutencao ligar texto:Limpando Banco de Dados`

---

## 🔎 Detalhes Técnicos

<Card title="Como funciona o ping?" icon="network-wired">
  O zNyx utiliza um protocolo de query padrão do Minecraft (SLP). Certifique-se de que o seu servidor não está bloqueando conexões externas de ping na porta configurada.
</Card>
