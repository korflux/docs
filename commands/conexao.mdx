---
title: "Automação e IA (/conexao)"
description: "Conecte seu servidor ao mundo externo através de fluxos n8n e Inteligência Artificial."
---

<Tooltip tip="Recurso exclusivo do Plano Pro">**RECURSO EXCLUSIVO DO PLANO PRO**</Tooltip>

O comando `/conexao` é a ponte tecnológica que transforma o Discord em um hub de automação. Ele permite que as mensagens enviadas em canais específicos sejam capturadas pelo zNyx e encaminhadas instantaneamente para Webhooks externos (como **n8n**, **Make** ou **Zapier**).

Essa funcionalidade é o que permite a criação de Agentes de IA customizados que leem e respondem sua comunidade, ou o registro externo de dados para análise de marketing avançada.

---

## 🔗 Configurando Conexões

### `adicionar`
Vincula um canal do Discord a um endpoint de Webhook. 

*   **Fluxo de Segurança:** Após executar o comando, o bot solicitará a URL do seu Webhook em uma conversa privada, garantindo que seu endpoint não seja exposto publicamente.
*   **Parâmetros:**
| Parâmetro | Tipo | Obrigatório | Descrição |
| :--- | :--- | :---: | :--- |
| `canal` | Canal (#) | Sim | O canal de texto que será monitorado. |
| `modo` | Escolha | Sim | Define o gatilho de envio (ver abaixo). |

*   **Modos de Captura:**
    1.  **`mentions`:** O bot só encaminha a mensagem para o Webhook se ele for mencionado (@zNyx). Ideal para Agentes de Suporte via IA.
    2.  **`all`:** Todas as mensagens enviadas no canal são encaminhadas. Útil para moderação via IA ou logs externos.

---

### `listar`
Exibe um painel com todas as conexões ativas no seu servidor.

*   **Utilidade:** Permite identificar rapidamente quais canais estão enviando dados e para onde. Útil para auditorias de segurança.

---

## 🤖 Cenários de Automação (Casos de Uso)

As possibilidades são ilimitadas, mas aqui estão as integrações mais comuns feitas pelos nossos clientes:

<AccordionGroup>
  <Accordion title="Agente de Suporte via ChatGPT">
    Configure um canal `#duvidas-ia` no modo `mentions`. No seu n8n, receba a mensagem, envie para a API da OpenAI com o contexto do seu servidor, e use o zNyx via Webhook para responder ao jogador automaticamente.
  </Accordion>
  <Accordion title="Moderação de Toxicidade">
    Configure um canal importante no modo `all`. Envie todas as mensagens para uma IA de análise de sentimento. Se uma mensagem for detectada como tóxica, sua automação pode alertar a Staff ou aplicar punições.
  </Accordion>
</AccordionGroup>

<Warning>
  O zNyx é o facilitador do envio. A lógica de processamento, resposta e armazenamento dos dados deve ser construída por você na plataforma de automação escolhida.
</Warning>

---

## ⚙️ Permissões Necessárias

| Operação | Cargos Sugeridos | Permissão Necessária |
| :--- | :--- | :--- |
| Configurar Conexão | Administradores | Administrador |
| Ver Conexões | Administradores | Administrador |

<Note>
  Por lidar com o tráfego de dados e endpoints externos, todos os comandos de integração exigem a permissão de **Administrador**. O zNyx não armazena o histórico das mensagens que passam pelo Webhook.
</Note>

