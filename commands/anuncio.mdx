---
title: "Anúncios e Vendas (/anuncio)"
description: "Sistema profissional para comunicados visuais e automação de cobranças via PIX."
---

O sistema de anúncios do zNyx foi arquitetado para substituir o uso de Webhooks manuais e complexos, oferecendo uma interface simplificada para criar mensagens visualmente impactantes e funcionais. No mundo dos servidores de Minecraft, a forma como você comunica novidades e ofertas define sua taxa de conversão e o engajamento da sua base.

Este módulo não é apenas informativo; ele é uma ferramenta de vendas poderosa, permitindo desde o envio de um simples aviso até a geração de cobranças profissionais com botões de interação.

---

## 📢 Comunicação Visual

### `padrao`
O subcomando principal para o envio de comunicados. Ao contrário de comandos de texto simples, o `padrao` abre um **Modal (Formulário)** para que você digite com calma o conteúdo do seu anúncio.

*   **Destaque:** Suporta formatação Markdown completa (negrito, listas, links) e oferece uma pré visualização organizada.
<ParamField query="cor" type="String (HEX)">
  Sobrescreve a cor padrão do bot para este anúncio específico.
</ParamField>

*   **Exemplo de Uso:**
```bash
/anuncio padrao cor:#FF0000
```

---

### `editar`
<Tooltip tip="Recurso exclusivo do Plano Pro">**RECURSO EXCLUSIVO DO PLANO PRO**</Tooltip>

Errou uma vírgula ou o preço mudou? O subcomando `editar` permite que você altere o conteúdo de um anúncio já enviado sem precisar deletá-lo.

*   **Vantagem Pro:** Mantém as reações e o histórico da mensagem original, mantendo a credibilidade do anúncio.
<ParamField query="mensagem_id" type="Snowflake (ID)" required>
  O ID da mensagem que o bot enviou anteriormente.
</ParamField>

---

## 💰 Automação de Vendas

### `pix`
Transforme seu canal de anúncios em um checkout de alta conversão. Este comando gera um card visual com todas as informações necessárias para um pagamento rápido.

*   **Redundância e Facilidade:** O bot gera um botão de **"Copiar Chave PIX"**. Quando o jogador clica, a chave é enviada isoladamente no chat, permitindo que ela seja copiada com um único toque, especialmente útil para jogadores no celular.
<ParamField query="valor" type="String" required>
  O preço do produto (ex: R$ 50,00).
</ParamField>

<ParamField query="chave" type="String" required>
  Sua chave PIX (E-mail, CPF, Aleatória, etc).
</ParamField>

*   **Exemplo de Uso:**
```bash
/anuncio pix valor:50,00 chave:vendas@servidor.com
```

---

### `promo`
<Tooltip tip="Recurso exclusivo do Plano Pro">**RECURSO EXCLUSIVO DO PLANO PRO**</Tooltip>

Crie eventos de vendas relâmpago com gatilhos de urgência.

*   **Marketing embutido:** O comando gera um card estilizado que destaca a porcentagem de desconto e o código do cupom, incentivando a compra imediata.
<ParamField query="desconto" type="String" required>
  O percentual de desconto (ex: 20%).
</ParamField>

<ParamField query="cupom" type="String" required>
  O código que o jogador deve usar na loja.
</ParamField>

---

## 🚀 Guia de Boas Práticas

<Steps>
  <Step title="Markdown">
    Use `### Título` para criar cabeçalhos dentro do seu anúncio e organizar as informações importantes.
  </Step>
  <Step title="Identidade Visual">
    No `/anuncio pix`, use a cor da sua loja para passar mais profissionalismo no momento do pagamento.
  </Step>
  <Step title=" IDs de Mensagem">
    Para usar o `editar`, ative o 'Modo Desenvolvedor' no seu Discord para copiar IDs facilmente.
  </Step>
</Steps>

---

## 🛡️ Permissões e Segurança

| Operação | Cargos Sugeridos | Permissão Necessária |
| :--- | :--- | :--- |
| Enviar Anúncio | Administradores | Administrador |
| Editar Mensagem | Administradores | Administrador |
| Gerar PIX | Administradores | Administrador |

<Note>
  Por motivos de segurança e para garantir a integridade financeira e visual do servidor, todos os comandos do módulo de anúncios exigem que o usuário possua a permissão de **Administrador**.
</Note>

